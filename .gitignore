.DS_Store
.idea/

# Root env
.env

# Backend env and config files
backend/.env
backend/.env.backup
backend/.env.production
backend/.phpactor.json
backend/.phpunit.result.cache
backend/.fleet/
backend/.idea/
backend/.nova/
backend/.phpunit.cache
backend/.vscode/
backend/.zed/
backend/auth.json

# Backend build and vendor
backend/node_modules
backend/public/hot
backend/public/storage
backend/storage/*.key
backend/storage/pail
backend/vendor

# Backend build output: only track Vite manifest and compiled CSS/JS
# Ignore everything by default in build folder
backend/public/build/*
# Allow the manifest.json at the root of build
!backend/public/build/manifest.json
# Allow the assets folder itself
!backend/public/build/assets/
# Ignore everything inside assets by default
backend/public/build/assets/*
# Re-allow only compiled CSS and JS bundles
!backend/public/build/assets/*.css
!backend/public/build/assets/*.js

# Backend misc
backend/Homestead.json
backend/Homestead.yaml
backend/Thumbs.db
backend/public/html/
backend/public/assets/media/

# Do not track backend/.gitignore file itself locally
backend/.gitignore

# Frontend/asset sourcemaps generated by vendors/builds
backend/public/assets/**/*.map

# Demo views (not for production)
backend/resources/views/demo/

# Local notes/docs not for VCS
backend/IS_ACTIVE_COLUMN_FIX.md
