Hier kun je een **schema-only baseline** leggen na `php artisan migrate` (zie `docs/MIGRATION_SQUASH.md`).

Bestandsnamen zoals `baseline-pgsql.sql` staan in `.gitignore` tot het team bewust een baseline in versiebeheer wil.
